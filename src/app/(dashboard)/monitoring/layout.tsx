/**
 * @fileoverview Layout para el módulo de Monitoreo
 * @module app/(dashboard)/monitoring/layout
 */

export default function MonitoringLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
